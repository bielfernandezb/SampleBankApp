<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".presentation.SampleBankApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_bank"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_bank_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SampleBank">
        <activity
            android:name="com.bielfernandezb.samplebank.presentation.login.view.activity.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.transaction.view.activity.MainActivity" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
</manifest>